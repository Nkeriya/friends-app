<nav class="navbar navbar-expand-lg navbar-dark bg-dark" style="background-color: #e3f2fd;" >
  <%= link_to "Friends App", users_path, class:"navbar-brand"%>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">

        <% if user_signed_in? %>
            <li class="nav-item active">
                <%= link_to "Home", "#", class:"nav-link"%>
            </li>

            <li class="nav-item active">
                <%= link_to "Profile", user_path(current_user), class:"nav-link"%>
            </li>

            <li class="nav-item active">
                <%= link_to "Friend Requests", friend_request_path(current_user), class:"nav-link"%>
            </li>

            <li class="nav-item active">
                <%= link_to "Sign out", destroy_user_session_path, method: :delete, class:"nav-link"%>
            </li>

        <% else %>
            <li class="nav-item active">
                <%= link_to "Sign In", new_user_session_path, class:"nav-link"%>
            </li>

            <li class="nav-item active">
                <%= link_to "Sign Up", new_user_registration_path, class:"nav-link"%>
            </li>

        <% end %>
    </ul>

        <p style="margin:0 30px;float:right;color:#fff;"><%= ("@" + current_user.firstname.capitalize) if user_signed_in?%></p> 
        <%= form_tag search_page_path, method: :GET, class:"form-inline my-2 my-lg-0" do %>           
            <%= search_field_tag :search, params[:search], placeholder: "Search user", class:"form-control mr-sm-2"%>  
            <%= button_tag "search", class:"btn btn-outline-success my-2 my-sm-0"%>
        <% end %>
  
  </div>
</nav>





       